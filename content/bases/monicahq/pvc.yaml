apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: monicahq-data
spec:
  storageClassName: cygnus-state
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
