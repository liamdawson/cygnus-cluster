apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mailhog-maildir
spec:
  storageClassName: cygnus-state
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
